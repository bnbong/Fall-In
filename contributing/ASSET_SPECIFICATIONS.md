# 그래픽 에셋 규격 가이드 (Asset Specifications)

이 문서는 "헤쳐 모여! (Fall In!)" 게임에 사용되는 그래픽 에셋의 규격을 정의합니다.

---

## 에셋 폴더 구조

| 에셋 종류 | 폴더 위치 | 파일명 규칙 |
|----------|----------|------------|
| 대대원카드 초상화 | `assets/images/characters/portraits/` | `portrait_{id}.png` |
| 면담 상반신 | `assets/images/characters/soldiers/` | `soldier_{id}.png` |
| 일반 mob 초상화 | `assets/images/characters/mobs/` | `portrait_mob_danger_{level}_{variant}.png` |
| 지휘관 캐릭터 | `assets/images/characters/commander/` | `commander.png` |
| 배경 이미지 | `assets/images/ui/backgrounds/` | `{scene}_background.png` |
| UI 아이콘 | `assets/images/ui/icons/` | `icon_{name}.png` |
| 카드 베이스 | `assets/images/cards/` | `battalion_card_base_single.png` |

---

## 대대원카드 (Battalion Card)

```
┌─────────────────────────────────────┐
│           150px x 253px              │
├─────────────────────────────────────┤
│                                      │
│    ┌─────┐  ← 카드번호 원 (28px Ø)   │
│    │ 번호 │    5.5% from top         │
│    └─────┘                           │
│                                      │
│      ┌─────────────┐                 │
│      │             │                 │
│      │  초상화 영역  │ ← 90px 지름     │
│      │  (원형 마스크) │   30% from top │
│      │             │                 │
│      └─────────────┘                 │
│                                      │
│        ─── 이름 ───    ← 55%         │
│        ─── 계급 ───    ← 66%         │
│        ─── 소속 ───    ← 76%         │
│        ─── 위험도 ───   ← 86%        │
│                                      │
└─────────────────────────────────────┘
```

### 카드 베이스

- **크기**: 150px × 253px
- **형식**: PNG (투명 배경)
- **파일**: `battalion_card_base_single.png`

### 카드 초상화

- **크기**: 90px × 90px (원형 마스크 적용)
- **위치**: 카드 중앙, 상단 30%
- **형식**: PNG (투명 배경, 정사각형)

---

## 병사 상반신 이미지 (Soldier Bust)

면담 장면에서 사용되는 병사 상반신 이미지입니다.

```
┌───────────────────────────────────────┐
│         600px x 700px (권장)           │
│                                        │
│          ┌─────────────┐              │
│          │    머리     │              │
│          │             │              │
│          │    상체     │ ← 중앙 정렬   │
│          │             │              │
│          │    허리     │              │
│          └─────────────┘              │
│                  ↑                     │
│            하단 = 허리선               │
└───────────────────────────────────────┘
                  │
            앵커 포인트 (테이블 가장자리)
```

### 규격

- **소스 크기**: 600px × 700px (권장)
- **표시 크기**: 400px × 480px
- **앵커 Y**: 465px (테이블 가장자리)
- **형식**: PNG (투명 배경)
- **파일명**: `soldier_{id}.png` (예: `soldier_1.png`, `soldier_55.png`)

### 중요 사항

- 캐릭터는 이미지 중앙에 배치
- 하단이 허리 수준에서 잘리도록 구성
- 하단 고정 확대 애니메이션 (0.3x → 1.0x)

---

## 지휘관 캐릭터 (Commander)

게임 화면 좌측에 표시되는 지휘관 캐릭터입니다.

### 규격

- **크기**: 250px × 680px
- **위치**: X=100, Y=520 (하반신이 화면 밖으로 잘림)
- **형식**: PNG (투명 배경)
- **파일**: `commander.png`

---

## 배경 이미지 (Backgrounds)

### 규격

- **크기**: 1280px × 720px (화면 해상도)
- **형식**: PNG 또는 JPG
- **파일**: `{scene}_background.png`

### 필요한 배경

| 파일명 | 설명 |
|--------|------|
| `title_background.png` | 타이틀 화면 |
| `main_game_background.png` | 메인 게임 화면 |
| `meetingroom_background.png` | 면담실 (모집) 화면 |
| `gameover_background_{id}.png` | 게임 오버 시나리오 별 게임 오버 화면 |
| `intro.png` or 별도의 애니메이션 GIF? | 게임 인트로 소개 화면 |

---

## 냉동식품 아이콘 (Frozen Food)

면담 장면에서 테이블 위에 표시되는 BX 냉동식품 아이콘입니다.

### 규격

- **크기**: 40px × 40px (기본값, config에서 조정 가능)
- **형식**: PNG (투명 배경)
- **위치**: 테이블 중앙 X=500, Y=530 (config에서 조정 가능)

### 필요한 에셋

| 파일명 | 설명 |
|--------|------|
| `frozen_food_ramen.png` | 라면 |
| `frozen_food_dumpling.png` | 만두 |
| `frozen_food_kimbap.png` | 김밥 |
| `frozen_food_burger.png` | 햄버거 |
| `frozen_food_chicken.png` | 치킨 |
| `frozen_food_pizza.png` | 피자 |
| `frozen_food_tteokbokki.png` | 떡볶이 |

### Config 설정 (`config.py`)

```python
FROZEN_FOOD_SIZE = 40       # 아이콘 크기 (px)
FROZEN_FOOD_GAP = 10        # 아이템 간 간격 (px)
FROZEN_FOOD_TABLE_X = 500   # 테이블 중앙 X 위치
FROZEN_FOOD_TABLE_Y = 530   # 테이블 위 Y 위치
FROZEN_FOOD_MIN_COUNT = 1   # 최소 표시 개수
FROZEN_FOOD_MAX_COUNT = 5   # 최대 표시 개수
```

---

## UI 요소

### 상단바 (Top Bar)

- **위치**: Y = 15px
- **높이**: 70px
- **너비**: 화면 전체 (1280px)

### 버튼 (Buttons)

```
┌─────────────────────────────────┐
│         120px x 40px             │
│                                  │
│         버튼 텍스트               │
│                                  │
└─────────────────────────────────┘
     border-radius: 5px
```

### 권장 버튼 규격

- **크기**: 120px × 40px
- **모서리**: 5px 라운드
- **형식**: PNG (투명 배경) 또는 코드로 생성

### 아이콘 규격

| 용도 | 권장 크기 | 파일명 예시 |
|------|----------|------------|
| 메뉴 아이콘 | 32px × 32px | `icon_menu.png` |
| 작은 아이콘 | 24px × 24px | `icon_back.png` |
| 상태 아이콘 | 48px × 48px | `icon_danger.png` |

---

## 병사 스프라이트 시트 (Soldier Figure)

게임판에서 사용되는 병사 스프라이트입니다.

```
┌─────────────────────────────────────────────────┐
│                  960px x 252px                   │
│  ┌──────────┬──────────┬──────────┬──────────┐  │
│  │ Frame 1  │ Frame 2  │ Frame 3  │ Frame 4  │  │
│  │ 240x252  │ 240x252  │ 240x252  │ 240x252  │  │
│  └──────────┴──────────┴──────────┴──────────┘  │
└─────────────────────────────────────────────────┘
         표시 크기: 60px x 63px (4배 축소)
```

### 규격

- **시트 크기**: 960px × 252px
- **프레임 수**: 4
- **프레임 크기**: 240px × 252px
- **표시 크기**: 60px × 63px
- **형식**: PNG (투명 배경)
- **파일명**: `soldier_danger_{level}.png`

### 개별 병사 피규어 스프라이트 (수집된 병사용)

```
┌─────────────────────────────────────────────────┐
│                  960px x 252px                   │
│  ┌──────────┬──────────┬──────────┬──────────┐  │
│  │ Frame 1  │ Frame 2  │ Frame 3  │ Frame 4  │  │
│  │ 240x252  │ 240x252  │ 240x252  │ 240x252  │  │
│  └──────────┴──────────┴──────────┴──────────┘  │
└─────────────────────────────────────────────────┘
         표시 크기: 60px x 63px (4배 축소)
```

### 규격 (개별 병사)

- **시트 크기**: 960px × 252px
- **프레임 수**: 4
- **프레임 크기**: 240px × 252px
- **표시 크기**: 60px × 63px
- **형식**: PNG (투명 배경)
- **파일명**: `figure_soldier_{id}.png` (예: `figure_soldier_1.png`)

---

## 색상 팔레트

### 기본 색상

| 용도 | HEX | RGB |
|------|-----|-----|
| 공군 블루 | #1E3A5F | (30, 58, 95) |
| 라이트 블루 | #4682B4 | (70, 130, 180) |
| 샌드 베이지 | #F5EBDC | (245, 235, 220) |

### 위험도 색상

| 레벨 | 라벨 | HEX | RGB |
|------|------|-----|-----|
| 1 | 안전 | #2ECC71 | (46, 204, 113) |
| 2 | 관심 | #F1C40F | (241, 196, 15) |
| 3 | 주의 | #E67E22 | (230, 126, 34) |
| 5 | 매우위험 | #E74C3C | (231, 76, 60) |
| 7 | 매우위험 | #8E44AD | (142, 68, 173) |

---

## 체크리스트

에셋 제작 시 확인사항:
- [ ] 투명 배경 PNG 형식 사용
- [ ] 정확한 크기 준수
- [ ] 파일명 규칙 준수 (소문자, 언더스코어)
- [ ] 중앙 정렬 및 여백 확인
- [ ] 위험도별 차별화된 디자인 (해당 시)
